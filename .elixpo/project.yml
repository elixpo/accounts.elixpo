project:
  name: accounts.elixpo
  description: "Elixpo OAuth SSO Provider"
  stack: nextjs
  node_version: "20"

ci:
  lint_cmd: "npx eslint ."
  test_cmd: ""
  build_cmd: "npx @cloudflare/next-on-pages"

maintainers:
  - github: Circuit-Overtime
    skills: [frontend, nextjs, ui, cloudflare, d1]
    lead: true

gist:
  id: ""
  account: elixpoo
