# Elixpo Pull Request Review — AI assistant via Pollinations
# Model is read from .github/ci_config.py (LLM_MODEL_AGENT)
# Trigger: mention "@elixpoo" in an issue or PR comment
# Whitelist: Elixpo org members (Circuit-Overtime, anwe-ch, elixpoo, ez-vivek)
#
# The bot acts as @elixpoo (via CI_AGENT_TOKEN — a PAT on that account).
# All comments, reviews, commits, and PRs are attributed to @elixpoo.

name: Elixpo Pull Request Review

# Serialize per-PR/issue so rapid @elixpoo mentions don't create duplicate
# branches or PRs. cancel-in-progress=false because the bot might be mid-commit.
concurrency:
  group: elixpo-pr-${{ github.event.issue.number || github.event.pull_request.number }}
  cancel-in-progress: false

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  elixpo:
    # Skip if the triggering actor is elixpoo itself — loop prevention.
    if: |
      github.actor != 'elixpoo' && (
        (github.event_name == 'issue_comment' && github.event.comment.user.login != 'elixpoo' && contains(github.event.comment.body, '@elixpoo')) ||
        (github.event_name == 'pull_request_review_comment' && github.event.comment.user.login != 'elixpoo' && contains(github.event.comment.body, '@elixpoo')) ||
        (github.event_name == 'pull_request_review' && github.event.review.user.login != 'elixpoo' && contains(github.event.review.body, '@elixpoo')) ||
        (github.event_name == 'issues' && github.event.issue.user.login != 'elixpoo' && (contains(github.event.issue.body, '@elixpoo') || contains(github.event.issue.title, '@elixpoo')))
      )
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    env:
      POLLINATIONS_TOKEN: ${{ secrets.CI_POLLINATIONS_KEY }}
      POLLINATIONS_TOKEN_CODER: ${{ secrets.CI_POLLINATIONS_KEY_CODER }}
      AGENT_TOKEN: ${{ secrets.CI_AGENT_TOKEN }}
    steps:
      - name: Check Whitelist
        id: whitelist
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.CI_AGENT_TOKEN }}
          script: |
            let body = '';
            if (context.eventName === 'issue_comment' || context.eventName === 'pull_request_review_comment') {
              body = context.payload.comment?.body || '';
            } else if (context.eventName === 'issues') {
              body = (context.payload.issue?.body || '') + (context.payload.issue?.title || '');
            } else if (context.eventName === 'pull_request_review') {
              body = context.payload.review?.body || '';
            }

            if (!body.includes('@elixpoo')) {
              console.log('Strict check failed: "@elixpoo" must be lowercase.');
              core.setOutput('authorized', 'false');
              return;
            }

            // Reject substring-only matches (e.g. @elixpoooo, @elixpoo-bot).
            if (!/(^|[^A-Za-z0-9_.+-])@elixpoo([^A-Za-z0-9_.+-]|$)/.test(body)) {
              console.log('@elixpoo substring only, not a real mention — skipping.');
              core.setOutput('authorized', 'false');
              return;
            }

            const whitelist = ['Circuit-Overtime', 'anwe-ch', 'elixpoo', 'ez-vivek'];
            const actor = context.actor;
            if (!whitelist.includes(actor)) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue?.number || context.payload.pull_request?.number,
                body: `Sorry @${actor}, only Elixpo org members can invoke @elixpoo.`
              });
              core.setOutput('authorized', 'false');
            } else {
              core.setOutput('authorized', 'true');
            }

      - name: Setup Bun
        if: steps.whitelist.outputs.authorized == 'true'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Checkout for config read
        if: steps.whitelist.outputs.authorized == 'true'
        uses: actions/checkout@v4
        with:
          path: config-src
          sparse-checkout: .github
          sparse-checkout-cone-mode: false

      - uses: actions/setup-python@v5
        if: steps.whitelist.outputs.authorized == 'true'
        with:
          python-version: "3.11"

      - name: Setup router + eyes reaction
        if: steps.whitelist.outputs.authorized == 'true'
        id: setup
        run: |
          # Eyes reaction on the triggering comment / review / issue
          if [ "${{ github.event_name }}" = "issue_comment" ]; then
            gh api repos/${{ github.repository }}/issues/comments/${{ github.event.comment.id }}/reactions -f content=eyes || true
          elif [ "${{ github.event_name }}" = "pull_request_review_comment" ]; then
            gh api repos/${{ github.repository }}/pulls/comments/${{ github.event.comment.id }}/reactions -f content=eyes || true
          elif [ "${{ github.event_name }}" = "issues" ]; then
            gh api repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/reactions -f content=eyes || true
          elif [ "${{ github.event_name }}" = "pull_request_review" ]; then
            gh api repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/reactions -f content=eyes || true
          fi

          cd config-src
          AGENT_MODEL=$(python3 -c "import sys; sys.path.insert(0,'.github'); from ci_config import LLM_MODEL_AGENT; print(LLM_MODEL_AGENT)")
          CODE_MODEL=$(python3 -c "import sys; sys.path.insert(0,'.github'); from ci_config import LLM_MODEL_CODE; print(LLM_MODEL_CODE)")
          THINK_MODEL=$(python3 -c "import sys; sys.path.insert(0,'.github'); from ci_config import LLM_MODEL_THINKING; print(LLM_MODEL_THINKING)")
          SEARCH_MODEL=$(python3 -c "import sys; sys.path.insert(0,'.github'); from ci_config import LLM_MODEL_SEARCH; print(LLM_MODEL_SEARCH)")
          cd ..

          echo "Router models: default=$AGENT_MODEL, background=$CODE_MODEL, thinking=$THINK_MODEL, webSearch=$SEARCH_MODEL"

          mkdir -p $HOME/.claude-code-router
          cp config-src/.github/ccr-adapters/*.js $HOME/.claude-code-router/ 2>/dev/null || true
          GEMINI_ADAPTER="$HOME/.claude-code-router/gemini-adapter.js"
          TOOL_PATCHER="$HOME/.claude-code-router/tool-schema-patcher.js"
          # Two providers — main (cheap key, gemini) + coder (separate key for
          # qwen-coder). Background subagent work (code-heavy) goes to coder
          # to keep costs split from the main agentic thread.
          #
          # Transformer order for coder: openai → tool-schema-patcher → maxtoken.
          # tool-schema-patcher backfills `parameters: {type:"object",...}` on any
          # tool missing it — qwen-coder rejects tool lists without it with a 400,
          # and WebSearch / some MCP tools ship without a parameters schema.
          cat > $HOME/.claude-code-router/config.json <<EOF
          {
            "LOG": false,
            "NON_INTERACTIVE_MODE": true,
            "API_TIMEOUT_MS": 600000,
            "transformers": [
              {"path": "${GEMINI_ADAPTER}"},
              {"path": "${TOOL_PATCHER}"}
            ],
            "Providers": [
              {
                "name": "pollinations-main",
                "api_base_url": "https://gen.pollinations.ai/v1/chat/completions",
                "api_key": "${POLLINATIONS_TOKEN}",
                "models": ["${AGENT_MODEL}", "${THINK_MODEL}", "${SEARCH_MODEL}"],
                "transformer": {
                  "use": ["gemini-adapter", "openai", ["maxtoken", {"max_tokens": 9000}]]
                }
              },
              {
                "name": "pollinations-coder",
                "api_base_url": "https://gen.pollinations.ai/v1/chat/completions",
                "api_key": "${POLLINATIONS_TOKEN_CODER}",
                "models": ["${CODE_MODEL}"],
                "transformer": {
                  "use": ["openai", "tool-schema-patcher", ["maxtoken", {"max_tokens": 9000}]]
                }
              }
            ],
            "Router": {
              "default": "pollinations-main,${AGENT_MODEL}",
              "background": "pollinations-coder,${CODE_MODEL}",
              "thinking": "pollinations-main,${THINK_MODEL}",
              "webSearch": "pollinations-main,${SEARCH_MODEL}"
            }
          }
          EOF

          bunx @musistudio/claude-code-router start &
          for i in $(seq 1 120); do
            curl -s http://localhost:3456 >/dev/null && break
            sleep 0.5
            if [ "$i" = "120" ]; then
              echo "::error::claude-code-router did not come up within 60s"
              exit 1
            fi
          done
        env:
          GH_TOKEN: ${{ secrets.CI_AGENT_TOKEN }}

      - uses: actions/checkout@v4
        if: steps.whitelist.outputs.authorized == 'true'
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || (github.event.issue.pull_request && format('refs/pull/{0}/head', github.event.issue.number)) || github.sha }}
          repository: ${{ github.repository }}
          token: ${{ secrets.CI_AGENT_TOKEN }}

      # ─────────────────────────────────────────────────────────────────
      # Pull shared Claude assets (agents + hooks + config) from
      # elixpo/claudeOps.elixpo so every repo's bot has the same skills.
      # ─────────────────────────────────────────────────────────────────
      - name: Pull claudeops skills
        if: steps.whitelist.outputs.authorized == 'true'
        uses: actions/checkout@v4
        with:
          repository: elixpo/claudeOps.elixpo
          ref: main
          token: ${{ secrets.CI_AGENT_TOKEN }}
          path: .claudeops-src
          sparse-checkout: |
            agents
            hooks
            config
          sparse-checkout-cone-mode: false

      - name: Wire claudeops skills into .claude/
        if: steps.whitelist.outputs.authorized == 'true'
        run: |
          mkdir -p .claude
          if [ -d .claudeops-src/agents ]; then
            cp -r .claudeops-src/agents .claude/agents
            echo "Wired $(ls .claude/agents | wc -l) agents from claudeops"
          fi
          if [ -d .claudeops-src/hooks ]; then
            cp -r .claudeops-src/hooks .claude/hooks
            echo "Wired $(ls .claude/hooks | wc -l) hooks from claudeops"
          fi
          if [ -f .claudeops-src/config/hooks.json ] && [ ! -f .claude/hooks.json ]; then
            cp .claudeops-src/config/hooks.json .claude/hooks.json
            echo "Wired hooks.json from claudeops"
          fi

      # ─────────────────────────────────────────────────────────────────
      # Pull the pre-built repo-context artifact (.elixpo-context/context.md)
      # produced by artifact-update.yml on every push to main. The model
      # reads this FIRST so it doesn't burn turns on `ls` / `gh issue view`
      # / tree-walking to reconstruct context we already have on disk.
      # ─────────────────────────────────────────────────────────────────
      - name: Download repo-context artifact
        if: steps.whitelist.outputs.authorized == 'true'
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: repo-context
          workflow: artifact-update.yml
          path: .elixpo-context
          if_no_artifact_found: warn
          github_token: ${{ secrets.CI_AGENT_TOKEN }}

      - uses: anthropics/claude-code-action@v1
        if: steps.whitelist.outputs.authorized == 'true'
        env:
          ANTHROPIC_BASE_URL: http://localhost:3456
          GIT_AUTHOR_NAME: "elixpoo"
          GIT_AUTHOR_EMAIL: "269200728+elixpoo@users.noreply.github.com"
          GIT_COMMITTER_NAME: "elixpoo"
          GIT_COMMITTER_EMAIL: "269200728+elixpoo@users.noreply.github.com"
        with:
          anthropic_api_key: "ccr-pollinations"
          show_full_output: true
          github_token: ${{ secrets.CI_AGENT_TOKEN }}
          trigger_phrase: "@elixpoo"
          branch_prefix: "elixpo/"
          # No --max-turns cap. Non-Anthropic models (gemini, qwen-coder) need
          # more headroom to work through multi-step tasks reliably; capping at
          # 15 was causing the model to give up mid-flow. The prompt below
          # breaks work into explicit tasks and paces via tracking-comment
          # updates, which matches the working pattern in issue-auto-fix.yml.
          claude_args: |
            --permission-mode bypassPermissions
          prompt: |
            Event: ${{ github.event_name }}
            Actor: @${{ github.actor }}
            Repo: ${{ github.repository }}
            Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
            Issue/PR number: ${{ github.event.issue.number || github.event.pull_request.number }}

            You are elixpo, a developer on the Elixpo team. Never mention Claude,
            Claude Code, AI, or LLMs — you are just a team member. Be casual,
            concise, and human.

            ### Available subagents (in .claude/agents/)
            Pulled at runtime from elixpo/claudeOps.elixpo. Delegate specialized tasks:
            architect, brancher, breaker, design-critic, fuzzer, judge, prism,
            questioner, red-team, refiner, researcher, specwriter, test-auditor,
            ui-architect, wirer. Use them via the Task tool for genuinely hard
            work — e.g. red-team for deep security review, architect for complex
            design, refiner for tightening the final PR description. Skip them
            for LGTMs and small fixes — they add turns.

            ### FIRST ACTION — rewrite the tracking comment
            The runtime posts a default "Claude Code is working..." comment.
            Find it and replace with a short on-brand message:

              ISSUE_OR_PR=${{ github.event.issue.number || github.event.pull_request.number }}
              COMMENT_ID=$(gh api "repos/${{ github.repository }}/issues/$ISSUE_OR_PR/comments" --paginate \
                --jq '[.[] | select(.user.login=="elixpoo" and ((.body|contains("Claude Code is working")) or (.body|contains("Claude finished"))))] | last | .id')
              gh api -X PATCH "/repos/${{ github.repository }}/issues/comments/$COMMENT_ID" \
                -f body="On it — picking this up now. Job run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

            Keep editing THIS SAME comment for progress updates. Do NOT post new
            comments. At each checkpoint below, replace the body with:

                ### 🔄 Working
                **Current step:** <what you're doing right now>

                **Progress:**
                - [x] <completed step>
                - [ ] <current step>
                - [ ] <upcoming step>

                _Job run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}_

                > Still working on it.

            ### Step 0 — Read the pre-built context FIRST
            A background workflow has already assembled a repo snapshot at
            `.elixpo-context/context.md` — project description, recent merged PRs,
            top-level tree, recently modified files, README + CLAUDE.md excerpts.

            Read that file ONCE before anything else. Do NOT run `ls`, `find`,
            `tree`, or walk directories to rediscover what's in there. Do NOT
            re-read README.md or CLAUDE.md unless the snippet is truncated.

            If `.elixpo-context/context.md` is missing (first run before artifact
            exists), fall back to `ls -la` — but this should be rare.

            ### Step 1 — Read the trigger
            For a PR event (pull_request_review_comment, pull_request_review, or
            issue_comment on a PR):
              PR_NUM=$(gh pr view --json number -q .number 2>/dev/null || echo "${{ github.event.pull_request.number }}")
              gh pr view $PR_NUM --comments
              gh pr diff $PR_NUM    # only if you need the diff
            For an issue event:
              gh issue view ${{ github.event.issue.number }} --comments

            Read ONCE. Don't re-read the same thing between turns.

            ### Step 2 — Break the request into concrete tasks
            Look at what the user actually asked and write out 2-5 concrete subtasks.
            Update the tracking comment with this breakdown before doing real work.
            Tasks should be specific (e.g. "edit src/lib/x.ts to add Y", not "think
            about the problem"). This breakdown is how you pace yourself — tick off
            tasks as you finish them.

            Then classify the request:
              - **REVIEW** — question, LGTM, code review, explanation, discussion.
                No code changes. Jump to Step 4 (final comment).
              - **CHANGE** — user asked for a code change (fix, add, update, refactor).
                Continue to Step 3.

            ### Step 3 — CHANGE path: branch, edit, biome, commit, push, PR

            Checkpoint updates required at each of these stages:

            1. **Branch**
               - If on a PR, you're already on the PR's branch — commit directly.
               - If on an issue (no PR yet), create one:
                   HEX=$(openssl rand -hex 2)
                   BRANCH="elixpo/issue-${{ github.event.issue.number }}-$HEX"
                   git checkout -b "$BRANCH"
               NEVER commit to main. Verify with `git rev-parse --abbrev-ref HEAD`.

            2. **Edit** — make minimal, scoped changes. Follow existing conventions
               (check biome config, tsconfig, neighbouring code style). Don't refactor
               unrelated code. No side quests.

            3. **Biome** — run `./biome.sh` (safe + unsafe fixes, then ci check).
               If ci still fails, read output, fix remaining issues by hand, then
               `./biome.sh ci` — must exit 0 before commit.

            4. **Commit + push**
                 git add -A
                 git commit -m "<concrete summary> (#<n>)"
                 git push -u origin HEAD

            5. **PR** — if you created a NEW branch (not working on an existing PR),
               open a ready (not draft) PR:
                 BODY="Hey @${{ github.actor }}, here's the PR.

                 ## 🎯 What this does
                 <2-4 sentences, user-visible outcome>

                 ## 💡 Why
                 <1-2 sentences referencing the trigger>

                 ## 🛠️ Changes
                 - \`<file>\`: <what changed and why>
                 - <3-6 bullets>

                 ## 🧪 How I tested it
                 - <biome ci clean / tests pass / dev-server verified / not verified>

                 ---
                 Fixes #${{ github.event.issue.number }}"

                 gh pr create --base main --head "$BRANCH" \
                   --title "[ELIXPO] <title>" --body "$BODY"

            ### Step 4 — Final tracking-comment edit
            Replace the tracking comment body with the final answer. Remove the
            `> Still working on it.` line. Use this format:

                ### ✅ Done
                <1-2 sentence summary>

                ### 📝 Changes  (omit if REVIEW path)
                - <file path>: <what changed and why>
                - <up to 6 bullets>

                ### 🔗 Links  (omit if REVIEW path)
                - Pull request: #<N>
                - Branch: `<branch>`
                - Workflow run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

                ### 🧪 Verification  (omit if REVIEW path)
                - biome ci clean / tests pass / not verified

                ### 👀 Review notes  (REVIEW path only)
                - <up to 4 bullets: Security / Logic / Quality / Nit>

            Update via:
              gh api -X PATCH "/repos/${{ github.repository }}/issues/comments/$COMMENT_ID" \
                -f body="$FINAL_BODY"

            ### Hard rules
            - NEVER commit to main.
            - All code changes go through a PR.
            - `./biome.sh` must exit 0 before every commit.
            - Stay strictly in scope.
            - Never say "Claude", "Claude Code", "AI", "LLM", "analyzing", "analysis".
              Speak as a teammate: "looking into this", "pushed a fix", "opened #N".
          additional_permissions: |
            actions: read

      # ─────────────────────────────────────────────────────────────────
      # Post-strip the "Claude finished …" / "Generated with Claude Code"
      # footer the runtime appends. Cheaper to do here in bash than to
      # make the model re-fetch + PATCH mid-flow (that was 2-3 wasted turns).
      # ─────────────────────────────────────────────────────────────────
      - name: Strip Claude branding from tracking comment
        if: always() && steps.whitelist.outputs.authorized == 'true'
        env:
          GH_TOKEN: ${{ secrets.CI_AGENT_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set +e
          ISSUE_OR_PR="${{ github.event.issue.number || github.event.pull_request.number }}"
          [ -z "$ISSUE_OR_PR" ] && exit 0

          # Most recent elixpoo comment that still has Claude branding.
          COMMENT_ID=$(gh api "repos/$REPO/issues/$ISSUE_OR_PR/comments" --paginate \
            --jq '[.[] | select(.user.login=="elixpoo" and ((.body|contains("Claude finished")) or (.body|contains("Claude Code")) or (.body|contains("Generated with Claude"))))] | last | .id')
          if [ -z "$COMMENT_ID" ] || [ "$COMMENT_ID" = "null" ]; then
            echo "No Claude-branded comment found — nothing to strip."
            exit 0
          fi

          BODY=$(gh api "repos/$REPO/issues/comments/$COMMENT_ID" --jq .body)
          CLEANED=$(printf '%s' "$BODY" \
            | sed -E '/Claude finished/d; /Generated with Claude/d; /Claude Code is working/d; /\[Create .* PR .*\]/d; /^---[[:space:]]*$/d' \
            | sed -e 's/[[:space:]]*$//')

          if [ "$BODY" != "$CLEANED" ]; then
            gh api -X PATCH "/repos/$REPO/issues/comments/$COMMENT_ID" -f body="$CLEANED" >/dev/null
            echo "Stripped Claude branding from comment $COMMENT_ID"
          else
            echo "Comment $COMMENT_ID already clean."
          fi

      # ─────────────────────────────────────────────────────────────────
      # Post-guard: if the bot committed to a branch but didn't open a PR,
      # open one here so the issue's Development tab is always populated.
      # ─────────────────────────────────────────────────────────────────
      - name: Ensure PR exists for any new elixpo/* branch
        if: steps.whitelist.outputs.authorized == 'true' && github.event_name == 'issue_comment' && !github.event.issue.pull_request
        env:
          GH_TOKEN: ${{ secrets.CI_AGENT_TOKEN }}
          REPO: ${{ github.repository }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
        run: |
          set -e

          BRANCHES=$(gh api "repos/$REPO/branches?per_page=100" --paginate \
            --jq ".[] | select(.name | startswith(\"elixpo/\") and contains(\"issue-$ISSUE_NUMBER\")) | .name")

          BRANCH=""
          for b in $BRANCHES; do
            OPEN_PR=$(gh pr list --repo "$REPO" --head "$b" --state open --json number --jq 'length')
            if [ "$OPEN_PR" = "0" ]; then
              BRANCH="$b"
              break
            fi
          done

          if [ -z "$BRANCH" ]; then
            echo "No elixpo/issue-$ISSUE_NUMBER-* branch without an open PR — nothing to do."
            exit 0
          fi
          echo "Found branch: $BRANCH (no open PR)"

          CHANGES=$(gh api "repos/$REPO/compare/main...$BRANCH" \
            --jq '.commits | map("- " + (.commit.message | split("\n")[0])) | join("\n")')

          ISSUE_AUTHOR=$(gh issue view "$ISSUE_NUMBER" --repo "$REPO" --json author --jq '.author.login')

          PR_BODY=$(cat <<EOF
          Hey @$ISSUE_AUTHOR, opened this PR automatically from the branch I pushed.

          ## 🛠️ Changes
          $CHANGES

          ## 🧪 How to verify
          - Pull the branch and run \\\`npm run build\\\` / biome check.
          - Review the diff against the issue's expected behavior.

          ---
          @$ISSUE_AUTHOR Fixes #$ISSUE_NUMBER
          EOF
          )
          PR_BODY=$(printf '%s\n' "$PR_BODY" | sed 's/^          //')

          PR_URL=$(gh pr create --repo "$REPO" \
            --head "$BRANCH" --base main \
            --title "[ELIXPO] $ISSUE_TITLE" \
            --body "$PR_BODY")
          echo "Opened PR: $PR_URL"

          PR_NUM=$(echo "$PR_URL" | grep -oE '[0-9]+$')
          gh issue comment "$ISSUE_NUMBER" --repo "$REPO" \
            --body "Opened #$PR_NUM with the changes."

          gh issue edit "$ISSUE_NUMBER" --repo "$REPO" --add-label ELIXPO || true

      # ─────────────────────────────────────────────────────────────────
      # Failure fallback: if anything errored, point to the failing run.
      # ─────────────────────────────────────────────────────────────────
      - name: Post failure notice
        if: failure() && steps.whitelist.outputs.authorized == 'true'
        env:
          GH_TOKEN: ${{ secrets.CI_AGENT_TOKEN }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set +e

          # Pull the tail of any failing-step logs so the comment actually says
          # what broke (e.g. "qwen-coder 400 missing parameters") instead of a
          # generic "something went wrong". gh run view --log-failed works
          # mid-run for steps that have already errored.
          ERROR_TAIL=$(gh run view "$RUN_ID" --repo "$REPO" --log-failed 2>/dev/null \
            | sed -E 's/\x1b\[[0-9;]*m//g' \
            | grep -iE "error|fail|400|401|403|404|429|500|502|503|bad_request|timeout|unauthorized" \
            | grep -vE "^\s*$" \
            | tail -8)
          if [ -z "$ERROR_TAIL" ]; then
            ERROR_TAIL="(no structured error captured — open the run logs)"
          fi
          # Strip any accidental token leakage and keep it under GitHub's
          # comment size sanity limit.
          ERROR_TAIL=$(printf '%s' "$ERROR_TAIL" | head -c 2000)

          MSG=$(cat <<EOF
          > ⚠️ Something went wrong on my end while handling this.

          <details>
          <summary><b>Error tail from the failing step</b></summary>

          <pre>
          $ERROR_TAIL
          </pre>

          </details>

          **What you can do:**

          <kbd>🔄 [**Re-run this workflow**]($RUN_URL)</kbd> &nbsp;·&nbsp; [View logs]($RUN_URL) &nbsp;·&nbsp; Or comment \`@elixpoo retry\` once the cause is fixed

          <sub>Run ID: \`$RUN_ID\`</sub>
          EOF
          )

          if [[ "${{ github.event_name }}" == "issue_comment" ]] || [[ "${{ github.event_name }}" == "issues" ]]; then
            gh issue comment "${{ github.event.issue.number }}" --repo "$REPO" --body "$MSG"
          elif [[ "${{ github.event_name }}" == "pull_request_review_comment" ]]; then
            gh pr comment "${{ github.event.pull_request.number }}" --repo "$REPO" --body "$MSG"
          elif [[ "${{ github.event_name }}" == "pull_request_review" ]]; then
            gh pr comment "${{ github.event.pull_request.number }}" --repo "$REPO" --body "$MSG"
          fi
